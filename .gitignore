# Build outputs — reproducible via wat2wasm / cargo / native/build.sh.
# Exception: examples/**/*.wasm are tracked as runnable distributables.
*.wasm
!examples/**/*.wasm
.air/
native/gen/
native/*-native
air/target/
dist/
*.o
*.cwasm

# Local credentials and configuration.
.env

# Editor / OS noise
.DS_Store
*~
